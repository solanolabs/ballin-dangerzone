tddium:
  posgres:
  - "echo 'Hello World, Dude.'"
  - "cat hello 1"
  - "cat hello 2"
  hooks:
    pre_setup: # Setup new session
    - "bundle exec create_session $TDDIUM_SESSION_ID $SOLANO_DW_HOSTNAME $TDDIUM_REPO_ROOT"
