tddium:
  hooks:
    pre_setup: bash sleep 25 && echo slept_pre_setup
    worker_setup: bash sleep 5 && echo slept_pre_setup
    post_setup: bash sleep 10 && echo slept_post_setup
    post_worker: bash sleep 15 && echo slept_post_worker
    post_build: bash sleep 20 && echo slept_post_build
  tests:
  - "echo 'Hello World, Dude.'"
  - "cat hello"
  - "cat hello >> collect_me.txt"
  - "sleep 20"
  collect:
    repo:
      - "*.txt"
      - "artifacts/**"
